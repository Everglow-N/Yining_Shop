

const actions={
  
};

const mutations={
    RESULT(state,request){
        state.data=request
    }
};

const state={a:"home",data:[]};

const getters={};

export default {
    actions,
    mutations,
    state,
    getters,
}